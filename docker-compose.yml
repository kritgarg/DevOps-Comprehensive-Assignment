version: '3.8'
services:
  app:
    build: .
    ports:
      -"80:5000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/devops_db
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data: